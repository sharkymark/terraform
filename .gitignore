# Local .terraform directories
.terraform/

# Terraform state files and backups
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude override files as they are usually for local use only
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Exclude CLI configuration files
.terraformrc
terraform.rc

# Sensitive variables files (if you use them; do not commit secrets!)
*.tfvars
*.tfvars.json